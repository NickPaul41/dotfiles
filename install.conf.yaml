- clean: ['~']

- shell:
    - [sh pre-install.sh, Running pre installation script]

- link:
    ~/.bashrc: bashrc
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.zshrc: zshrc
    ~/.p10k.zsh: p10k.zsh
    ~/.tmux.conf: tmux.conf
